name: first_deployment
on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: linux

    strategy:
      matrix:
        Apache: [2.4.51]

    steps:
    - name: curl
      uses: wei/curl@master
      with:
        args: http://3.110.77.174/opt/deployment.cgi
        CI: true
